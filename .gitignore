# Read-only clone of the product site used as the architecture reference
reference
reference/

# Dependencies & build output (local only — Vercel builds from source)
node_modules/
.next/
*.tsbuildinfo

# Env files
.env
.env*.local

# Local verification output
verify-report.*.json
